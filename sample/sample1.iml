prog sample1(in m: int, out o: int) {
  def func1(in m: int, out o: int) {
    m := m * 5;
    o := m;
    o := o / 2;
  }
  def func2(out o: int) {} 

  var i :int;
  var c :int(0 .. 100);
  var a :int[50 .. 100];

  c := 50;
  a[50] := 10;

  //a Comment
  while(i < c) {
    a[i+50] := i;
  }

  if(i < 5) {
    i := i + 5;
  }
  else {
    i := i - 5;
  }

  func1(m, o);
}