prog sample1(in m: int, in n: int, out o: int) {
  def func1(in m: int, out o: int) {
    m := m * 5;
    o := m;
    o := o / 2;
  }
  def func2(in n: int, out o: int) {

  }

  var o1:int(0 .. 10);
  var array:int[0 .. 100];
  var x:int;
  x := array[0];

  if (o1) {
    var b: int;
    func1(m, o1);
  } else {
    func2(m, o1);
  }
  if (true = (false &? false)) {
    var hambbe: int;
  }
  var a: int;
  var b: int;
  o1 := b;
  while (a init > b init) {
    func1(m, o1);
    func3(m, o1);
  }
  var o2: int;
  func2(n, o2);
}