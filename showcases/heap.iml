prog makeHeap(inOut a : int[0..10]) {

    def getChildIndizes(in parentIndex : int(0..10), out childIndex1 : int, out childIndex2 : int) {
        childIndex1 := parentIndex * 2 + 1;
        childIndex2 := childIndex1 + 1;
    }

    var parentIndex : int(0..10);
    for(parentIndex) {
        var childIndex1 : int;
        var childIndex2 : int;
        var parentIndexParam : int;
        getChildIndizes(parentIndex, childIndex1, childIndex2);
    }

}